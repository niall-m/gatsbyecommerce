import styled from 'styled-components';

export const HeaderWrapper = styled.header`
  max-width: 1200px;
  margin: 0 auto;
  padding: 10px 20px;
  box-sizing: border-box;

  display: flex;
  flex-wrap: wrap;
`;